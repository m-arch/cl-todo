function submitTaskEdit (url, action){
    $.post(url, { action: action, title: $("#title").val() },
	   function(data){
	       if (data.success){
		   alert("success");
	       }else{
		   alert(data.message);
	       }
	   });
    return false;
}
